# BandSync AI Audio Analysis - System Flow

## 📱 User Flow

```
User Opens App
    ↓
Navigates to Library Tab
    ↓
Taps + Button
    ↓
Selects "AI Audio Analysis"
    ↓
Picks Audio File (MP3/WAV/etc)
    ↓
[Loading Screen - "Analyzing audio..."]
    ↓
Results Displayed:
├── 🎹 Key & Scale
├── ⏱️  Tempo (BPM)
├── 🎸 Chord Progression
├── 📊 Chord Timeline
├── 🤖 AI Insights (optional)
└── 🎵 Similar Songs (optional)
```

## 🔧 Technical Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     FLUTTER APP (Frontend)                   │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │  audio_analysis_screen.dart                        │    │
│  │  - File picker UI                                  │    │
│  │  - Loading indicator                               │    │
│  │  - Results display                                 │    │
│  └──────────────────┬─────────────────────────────────┘    │
│                     │                                        │
│  ┌──────────────────▼─────────────────────────────────┐    │
│  │  audio_analysis_service.dart                       │    │
│  │  - HTTP multipart upload                           │    │
│  │  - POST to /analyze endpoint                       │    │
│  └──────────────────┬─────────────────────────────────┘    │
└────────────────────┼──────────────────────────────────────┘
                     │
                     │ HTTP Request (audio file)
                     │
┌────────────────────▼──────────────────────────────────────┐
│              PYTHON BACKEND (Flask Server)                 │
│                                                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │  app.py                                            │  │
│  │  - Receives audio file                             │  │
│  │  - Saves temporarily                               │  │
│  │  - Calls AudioAnalyzer                             │  │
│  └──────────────────┬─────────────────────────────────┘  │
│                     │                                      │
│  ┌──────────────────▼─────────────────────────────────┐  │
│  │  audio_analyzer.py                                 │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 1. Load audio with librosa                   │ │  │
│  │  │    y, sr = librosa.load(audio_path)          │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 2. Detect Tempo                              │ │  │
│  │  │    tempo = librosa.beat.beat_track()         │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 3. Detect Key (Krumhansl-Schmuckler)         │ │  │
│  │  │    - Get chromagram                          │ │  │
│  │  │    - Compare to major/minor profiles         │ │  │
│  │  │    - Find best correlation                   │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  └──────────────────┬─────────────────────────────────┘  │
│                     │                                      │
│  ┌──────────────────▼─────────────────────────────────┐  │
│  │  chord_recognizer.py                               │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 1. Create chord templates                    │ │  │
│  │  │    - Major: [1, 0, 0, 0, 0.8, 0, 0, 0.9...] │ │  │
│  │  │    - Minor: [1, 0, 0, 0.8, 0, 0, 0, 0.9...] │ │  │
│  │  │    - 7th, Maj7, Min7 templates               │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 2. Get chromagram frames                     │ │  │
│  │  │    chroma = librosa.feature.chroma_cqt()     │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 3. Match each frame to chord template        │ │  │
│  │  │    score = dot(frame, template)              │ │  │
│  │  │    best_chord = max(scores)                  │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 4. Simplify sequence                         │ │  │
│  │  │    - Group consecutive same chords           │ │  │
│  │  │    - Filter short durations (< 0.5s)         │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  └──────────────────┬─────────────────────────────────┘  │
│                     │                                      │
│  ┌──────────────────▼─────────────────────────────────┐  │
│  │  ai_analyzer.py (Optional - needs API key)        │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 1. Build prompt with musical data            │ │  │
│  │  │    "Analyze: Key=C major, Chords=C-Am-F-G"  │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 2. Call OpenAI API                           │ │  │
│  │  │    response = client.chat.completions.create │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  │  ┌──────────────────────────────────────────────┐ │  │
│  │  │ 3. Get insights & similar songs              │ │  │
│  │  │    - Mood analysis                           │ │  │
│  │  │    - Genre suggestions                       │ │  │
│  │  │    - Playing tips                            │ │  │
│  │  └──────────────────────────────────────────────┘ │  │
│  └──────────────────┬─────────────────────────────────┘  │
│                     │                                      │
│  ┌──────────────────▼─────────────────────────────────┐  │
│  │  Return JSON Response                              │  │
│  │  {                                                 │  │
│  │    "tempo": 120.0,                                 │  │
│  │    "key": "C",                                     │  │
│  │    "scale": "major",                               │  │
│  │    "chord_progression": ["C", "Am", "F", "G"],    │  │
│  │    "chords_timeline": [...],                       │  │
│  │    "ai_insights": "...",                           │  │
│  │    "similar_songs": [...]                          │  │
│  │  }                                                 │  │
│  └──────────────────┬─────────────────────────────────┘  │
└────────────────────┼──────────────────────────────────────┘
                     │
                     │ JSON Response
                     │
┌────────────────────▼──────────────────────────────────────┐
│                  FLUTTER APP (Display)                     │
│                                                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │  Parse JSON & Update UI                            │  │
│  │  - Show key & tempo in info card                   │  │
│  │  - Display chord progression as chips              │  │
│  │  - Show timeline as list                           │  │
│  │  - Display AI insights in purple card              │  │
│  │  - List similar songs                              │  │
│  └────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

## 🎯 Key Algorithms

### 1. Krumhansl-Schmuckler Key Detection
```
Input: Audio waveform
    ↓
Extract chromagram (12 pitch classes)
    ↓
Average across time → pitch class distribution
    ↓
Correlate with 24 key profiles (12 major + 12 minor)
    ↓
Highest correlation = detected key
```

### 2. Template-Based Chord Recognition
```
Input: Chromagram frame
    ↓
For each chord template (C, Cm, C7, Cmaj7, etc.):
    Calculate dot product with frame
    ↓
Best match (highest score) = detected chord
    ↓
Filter: score < 0.3 → "No chord"
```

### 3. Chord Sequence Simplification
```
Input: [C, C, C, Am, Am, F, F, F, F, G, G]
    ↓
Group consecutive: [(C, 3), (Am, 2), (F, 4), (G, 2)]
    ↓
Filter short (< 0.5s): [(C, 3), (Am, 2), (F, 4), (G, 2)]
    ↓
Output: [C, Am, F, G] with timestamps
```

## 📊 Data Flow Example

```
Input: "song.mp3" (3 minutes, 120 BPM, Key of C major)
    ↓
Librosa loads: 44100 Hz sample rate, mono
    ↓
Tempo detection: 120.3 BPM
    ↓
Key detection: C major (correlation: 0.87)
    ↓
Chromagram: 12 x 1500 matrix (12 pitches, 1500 time frames)
    ↓
Chord recognition: 45 chord changes detected
    ↓
Simplification: 12 unique chords in progression
    ↓
AI analysis: "Upbeat pop progression, common in 80s music..."
    ↓
Similar songs: ["Don't Stop Believin'", "Sweet Child O' Mine", ...]
    ↓
Return to Flutter: Display in beautiful UI
```

## ⚡ Performance

- **Audio Loading**: 1-2 seconds
- **Feature Extraction**: 3-5 seconds
- **Chord Recognition**: 2-4 seconds
- **Key Detection**: 1 second
- **AI Analysis**: 2-3 seconds (if enabled)
- **Total**: 10-15 seconds average

## 🎨 UI Components

```
AudioAnalysisScreen
├── Upload Card
│   ├── Icon (audiotrack)
│   ├── Title
│   └── Upload Button
├── Loading Card (conditional)
│   ├── CircularProgressIndicator
│   └── "Analyzing audio..." text
├── Basic Info Card
│   ├── Key & Scale
│   ├── Tempo (BPM)
│   └── Duration
├── Chord Progression Card
│   └── Chips (C, Am, F, G...)
├── Chord Timeline Card
│   └── ListTiles with timestamps
├── AI Insights Card (optional)
│   ├── Psychology icon
│   └── AI-generated text
└── Similar Songs Card (optional)
    └── List of songs
```

## 🔐 Security Notes

- Audio files are temporarily stored and deleted after analysis
- OpenAI API key stored in .env (not committed to git)
- No audio data sent to OpenAI (only chord/key metadata)
- CORS enabled for Flutter web support
